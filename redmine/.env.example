# 通常は ./redminectl setup が乱数で生成するので手で作る必要はない。
# 手で作る場合: cp .env.example .env して値を埋める (.env は git 管理外)
REDMINE_DB_PASSWORD=change-me
# openssl rand -hex 64 で生成
REDMINE_SECRET_KEY_BASE=change-me
# ランタイムデータの置き場を変えるとき (既定 ~/srv/redmine)
#REDMINE_DATA_DIR=/Volumes/data/redmine
